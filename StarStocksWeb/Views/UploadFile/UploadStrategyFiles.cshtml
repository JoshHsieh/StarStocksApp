@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    ViewData["Title"] = "Upload Files";
}

@model StarStocksWeb.Frameworks.ViewModels.UploadFileViewModel

@section AddToHead
{
    <!--  修改 fileinput buuton text  -->
    <style type="text/css">
        .custom-file-input:lang(en) ~ .custom-file-label::after {
            content: "瀏覽" !important;
        }
    </style>
}

<div class="container-fluid">
    <!-- Page Heading -->
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0 text-gray-800">Upload Files</h1>
        @*<a href="#" class="d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm"><i class="fas fa-download fa-sm text-white-50"></i> Generate Report</a>*@
    </div>

    <div class="row">
        <div class="col">
            <div class="card-body">
                @using ( Html.BeginForm( "UploadStrategyFiles", "UploadFile", FormMethod.Post, new { id = "UploadForm", enctype = "multipart/form-data" } ) )
                {
                    <div class="form-row">
                        <div class="col-sm-9 offset-1 input-group mb-4">
                            <div class="custom-file ">
                                @Html.TextBoxFor( x => x.StrategyResult, new { name = "StrategyResult", type = "file", @class = "custom-file-input" } )
                                <label class="custom-file-label" for="StrategyResult">Choose Strategy Excel</label>
                            </div>
                        </div>
                    </div>

                    <p style="text-align: center">
                        <button type="submit" class="btn btn-primary btn-icon-split sharp" name="btnUpload" id="btnUpload">
                            <span class="icon text-white-50">
                                <i class="fas fa-search"></i>
                            </span>
                            <span class="text">搜尋</span>
                        </button>
                    </p>
                }
            </div>
        </div>
    </div>
</div>

@section scripts
{
    <script src="~/vendor/bs-custom-file-input.js"></script>
    <script type="text/javascript" language="javascript">
        $(document).ready(function () {
            bsCustomFileInput.init();

        });

    </script>
}
